import { createTheme } from "@material-ui/core";

const theme = createTheme({
    palette: {
        primary: {
          main: '#80d8ff',
        },
        secondary: {
          main: '#90caf9',
        },
      }
});

export default theme;
